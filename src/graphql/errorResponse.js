// TODO: Need to run this through a tool to get the proper graphQL error response.
// eslint-disable-next-line import/no-unused-modules
export const errorResponse = (error) => JSON.stringify(error, undefined, 4);
